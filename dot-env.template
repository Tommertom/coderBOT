# Environment Variables

# Your Telegram bot tokens from @BotFather, separated by commas
# You can specify one or more tokens to run multiple bot instances
# Example: TELEGRAM_BOT_TOKENS=1234567890:AABBCCDDEEFFGGHHIIJJKKLLMMNNOOPPQQrrss,9876543210:ZZYYXXWWVVUUTTSSRRQQPPOONNMMllkkjjii

# Comma-separated list of Telegram user IDs allowed to use the bot
# Example: ALLOWED_USER_IDS=123456789,987654321
ALLOWED_USER_IDS=

# Auto-kill bot on unauthorized access (true/false)
# If set to true, the bot will shutdown when an unknown user tries to access it
# All allowed users will be notified before shutdown
AUTO_KILL=true

# Xterm/PTY Configuration
# Maximum number of output lines to keep in buffer (default: 1000)
XTERM_MAX_OUTPUT_LINES=1000

# Session timeout in milliseconds (default: 1800000 = 30 minutes)
# Set to a high value like 86400000 (24 hours) to keep sessions alive longer
# Set to 315360000000 (10 years) to effectively disable timeout
XTERM_SESSION_TIMEOUT=1800000

# Terminal dimensions - rows (default: 50)
XTERM_TERMINAL_ROWS=50

# Terminal dimensions - columns (default: 100)
XTERM_TERMINAL_COLS=100

# Terminal font size (default: 14)
XTERM_FONT_SIZE=18

# Shell path for PTY sessions (default: /bin/bash)
XTERM_SHELL_PATH=/bin/bash

# Media folder to watch for files to send to users
# Files will be sent and then moved to a 'sent' subfolder
MEDIA_TMP_LOCATION=/tmp/coderBOT_media

# Clean up media directory on worker startup (true/false)
# If set to true, each bot worker will delete its media directory (including subdirectories)
# on startup, ensuring a clean state. Useful for development or fresh starts.
# WARNING: This will delete all files in {MEDIA_TMP_LOCATION}/bot-N/ directories
CLEAN_UP_MEDIADIR=false

# Message auto-delete timeout in milliseconds (default: 10000 = 10 seconds)
# Time to wait before automatically deleting confirmation messages
# Set to 0 to disable auto-deletion of messages
MESSAGE_DELETE_TIMEOUT=10000

# Auto-refresh Configuration
# Automatically refresh the last shown terminal screenshot after sending commands
# Refresh interval in milliseconds (default: 5000 = 5 seconds)
SCREEN_REFRESH_INTERVAL=5000

# Maximum number of automatic refreshes (default: 5)
# Total auto-refresh duration = SCREEN_REFRESH_INTERVAL * SCREEN_REFRESH_MAX_COUNT
SCREEN_REFRESH_MAX_COUNT=5

# Bot Token Monitoring
# Monitor .env file for changes to bot tokens and dynamically spawn/kill workers
# Check interval in milliseconds (default: 300000 = 5 minutes)
# Set to 0 to disable monitoring
BOT_TOKEN_MONITOR_INTERVAL=300000

# ============================================================================
# Control Bot Configuration (Optional)
# ============================================================================
# Administrative bot for managing worker bot processes
# If not set, controlBOT will not be instantiated
CONTROL_BOT_TOKEN=

# Control Bot Access (Required if CONTROL_BOT_TOKEN is set)
# Comma-separated list of Telegram User IDs who can use the control bot
# Example: CONTROL_BOT_ADMIN_IDS=123456789,987654321
CONTROL_BOT_ADMIN_IDS=

# Verbose Logging (Optional)
# When enabled, the ControlBOT will forward all console output (stdout/stderr)
# from child bot processes to its own console for debugging and monitoring
# Set to false to disable verbose console output (default: true)
VERBOSE_LOGGING=true

# Airplane Mode (Optional)
# When enabled (on/true/1), the bot will send terminal output as text messages
# instead of rendered images. This uses less bandwidth and is faster.
# In airplane mode, only the last 500 characters of terminal output are sent,
# sanitized from ANSI codes and control characters.
# Set to off/false/0 to use normal image rendering mode (default: off)
AIRPLANE_MODE=off


# ============================================================================
# Message Placeholders (Optional)
# ============================================================================
# Define text replacements for [m0] through [m9] placeholders in messages
# When you send a message containing [m0], it will be replaced with the value of M0
# Empty values are ignored and the placeholder will remain unchanged
# Example: M0=npm run build
M0=copy the generated file(s) to /tmp/coderBOT_media/bot-2
M1=
M2=
M3=
M4=
M5=
M6=
M7=
M8=
M9=

# ============================================================================
# Audio Transcription (Speech-to-Text) Configuration (Optional)
# ============================================================================
# API key for audio transcription service
# Supports OpenAI Whisper API (keys starting with "sk-") and Google Gemini API
# The bot will auto-detect which service to use based on the key format
# Leave empty to disable audio transcription feature
# OpenAI example: TTS_API_KEY=sk-proj-abcd1234...
# Gemini example: TTS_API_KEY=AIzaSy...
TTS_API_KEY=

# Default audio transcription mode (copy/prompt)
# copy: Transcribed text is returned as a formatted message for copy-pasting (default)
# prompt: Transcribed text is directly sent to active terminal session as a prompt
# Users can override this with the /audiomode command
AUDIO_TRANSCRIPTION_DEFAULT_MODE=copy

# ============================================================================
# AI Assistant Arguments (Optional)
# ============================================================================

COPILOT_ARGUMENTS=--allow-all-tools --deny-tool 'shell(rmdir)' --deny-tool 'shell(rm)' --deny-tool 'shell(sudo)'

# Arguments to pass to the gemini command when starting a session
GEMINI_ARGUMENTS=

# Arguments to pass to the opencode command when starting a session
OPENCODE_ARGUMENTS=

#
# CUSTOM ENV - not specific to coderBOT
# Place here your custom environment variables if needed
#